var React = require('react');

var NavHeader = React.createClass({
  render: function () {
    return (
      <div>
        HELLO FROM NAV HEADER
      </div>
    );
  }
});


module.exports = NavHeader;
